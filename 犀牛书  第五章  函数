5.1  声明式函数
   1什么叫声明式的函数？
     使用声明式/静态格式定义函数，
     声明式函数的格式：首先需要一个function关键字，接着是函数名称，放在圆括号内的零个或多个参数，然后就是函数体
     function functionname （有参数或者没有参数）{
     语句；
     }
5.2  匿名函数
     1 匿名函数是动态的，它运行时很有用而声明式函数是静态的，匿名函数每运行一次得从新创建一个对象，在循环体也是一样的，
      匿名函数的语法：  var variable = new Function ("a1", "a2", "a3", ..., "an", "function body");
      第一个参数是在定义声明式函数指定的第一个参数。最后一个是参数的函数体，把整个函数赋予给一个变量。
      例如： var fun = new function ("x", "y", "return x * y");
5.3  函数字面量   （也叫函数表达式）
     例如1（）：
     var fun = function ()
{
    return  x × Y；
}；
     例如2(非匿名形式，可以带有名称的函数不过  这个函数只能在函数内部调用，除非调用一个递归函数)：
     var fun = function mu (x, y){
       return x, y;
     };
    
       
